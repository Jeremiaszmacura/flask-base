name: CI

on:
  push:

jobs:
  unit-testing:
    name: Unit Tests
    uses: ./.github/workflows/unit-tests.yml
    with:
      job: unit-testing
    secrets: inherit

    lint-code:
    name: Lint code
    uses: ./.github/workflows/lint-code.yml
    with:
      job: lint-code
    secrets: inherit
